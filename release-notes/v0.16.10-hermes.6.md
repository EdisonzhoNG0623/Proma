# Proma 0.16.10-hermes.6

这是基于官方最新 Release `v0.16.10` 之后的未发布上游 `main@0e4d2f9b`（Pi-only runtime）移植的 Hermes Remote 实验性 prerelease。版本号延续既有 Hermes fork 预览线，避免占用上游 `main` 清单中的未发布开发版本号。

## 主要变化

- 保持 Proma 本地 Agent 为 Pi-only；Hermes 在主进程 service 边界作为显式 external runtime 分流。
- 支持 Hermes Dashboard 与 API Server 两种显式协议，不在协议之间或向本地 Agent 静默降级。
- 支持 Direct / SSH Tunnel、target-scoped 凭据、持久 Cookie partition、SSH host-key challenge 与受限 SFTP 文件浏览。
- 支持远端项目、远端会话、canonical REST history、实时流式输出和真实远端停止。
- 支持 approval、clarify、sudo、secret 交互卡片。
- 支持图片和普通文件的原子 turn 提交、历史媒体恢复、Proma 内预览和系统默认程序打开。
- 远端 snapshot 是历史真源；Hermes 不读取或追加本地 Agent JSONL。
- 保留侧边栏双区域独立滚动、可拖拽 split-pane、Target 快速切换及可操作错误状态。
- 保留超长本地 Agent 会话优化：轻量 completion、增量 tail reconcile、Renderer-safe snapshot、20 组首屏分页、工具索引缓存和几何扫描合帧。

## 验证

- Workspace TypeScript typecheck：通过。
- 96 个测试文件逐文件隔离执行：806 pass / 0 fail。
- Electron production build：通过。
- Windows dev smoke：主 Renderer 加载成功；Hermes preload API、turn submit state API、增量历史 API 和 Hermes 侧边栏均存在。

## Windows 安装提示

安装包未代码签名，Windows SmartScreen 可能显示“未知发布者”。请在安装前核对 Release 页面提供的 SHA-256。
